{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Create new message</h1>

        {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_row(form.content) }}
        {{ form_row(form.category) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
{% extends 'base.html.twig' %}

{% block body %}

    {% include 'navbar.html.twig' %}
    <div class="container">
        <h1>Edit reaction</h1>
        {{ form_start(editReactionForm)  }}
        {{ form_row(editReactionForm.content, { 'label': 'Edit your reaction' } ) }}
        {{ form_row(editReactionForm.React, { 'label': 'Edit' }) }}
        {{ form_end(editReactionForm) }}
    </div>
{% endblock %}